Review the linked file which has general recommendations for our app, but note that it was created without any insights into the app or the codebase-graph files - docs/project_plans/visualization-improvements.md. The general idea is to enhance the viewing features of the app so that it is easier to understand the details being shown, rather than just a mess of spaghetti. One example we're emulating is FalkorDB. Create a plan (as a file in docs/project_plans/) for enhancements to the visualization functionality in order to add new features, such as layering etc which adds detail as a user zooms in, expanding clusters into the containing Nodes/Edges, etc. Especially consider the hierarchy idea to have multiple layers, with more details containing to be shown as the user zooms in further to a given cluster, adding the concept of clusters (or similar) as entities in the graph map, containing a set of Nodes, Edges, and other clusters. Additionally, consider any additional data or updates to the scraping process to best enable this process. I've moved the full set of scraping scripts to the project in code_map/ to unify the various aspects of the app and allow making edits directly to the scripts when needed. Only create your plan for now, but it should be a detailed and comprehensive plan. Also note, you can use the various skills in the project for extra guidance, tips/tricks, optimizations, and other insights around WebGL/ThreeJS and D3.